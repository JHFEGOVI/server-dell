# Grafana
# Set a strong password before starting the stack for the first time.
# Changing this value after initialization has no effect — use the Grafana CLI instead.
GRAFANA_ADMIN_PASSWORD=your_strong_password_here
